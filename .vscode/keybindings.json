//Not sure if it is needed to move it to real config folder
//under host machine under ~/.config/Code/User/keybindings.json
[
    {
        "key": "ctrl+shift+e",
        "command": "workbench.action.tasks.runTask",
        "args": "C/C++: g++ build and run with example"
    }
    {
        "key": "ctrl+shift+/",
        "command": "workbench.action.tasks.runTask",
        "args": "C/C++: g++ build and run with input"
    }
]